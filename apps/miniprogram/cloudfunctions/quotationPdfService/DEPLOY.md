# quotationPdfService 部署配置

该云函数会加载中文字体并生成 PDF，首次调用时间超过普通云函数默认的 3 秒限制。

部署要求：

- 使用“上传并部署：云端安装依赖”。
- 超时时间设置为 20 秒或以上。
- 运行内存建议设置为 512MB。
- 测试环境和生产环境需要分别配置。

若小程序出现 `cloud.callFunction:fail errCode: -504003`，表示函数在配置的时间内没有执行完成，请先检查超时时间配置。
