# 杭州亿辉文化创意项目 - 全局 AI 编程规则

本文档为本项目唯一的 AI 编程规范与规则指南，所有 AI 编程工具（Antigravity、Cursor、Windsurf、Claude Code、Copilot 等）必须严格遵守以下所有规则。

---

## 一、 技术栈规范（锁死与禁止项）

1. **Web 管理后台 (`apps/admin-web`)**：
   - Vue3 + `<script setup>` + Element Plus + JavaScript + Axios + Less
   - **严禁使用**：TypeScript、Vue2 选项式 API、React、TailwindCSS 或其他第三方 UI 库。
2. **微信小程序端 (`apps/miniprogram`)**：
   - 原生微信小程序 + TDesign 小程序组件库 + WXML + WXSS + JavaScript
3. **后端云函数 (`apps/miniprogram/cloudfunctions`)**：
   - Node.js 腾讯云函数（微信小程序 SDK / RESTful 接口）
4. **AI 功能**：
   - 仅对接 Gemini API
5. **整体禁止项**：
   - 禁止引入 TypeScript、独立服务器框架、未授权的第三方 UI 库或大模型 SDK。

---

## 二、 核心业务与架构原则

1. **业务判断与展示文案分离（强制）**：
   - **禁止**使用中文展示文案作为业务判断、状态比较、权限判断、分支条件或数据库枚举值。
   - 所有业务判断必须使用稳定的英文枚举值、布尔值、ID 或常量（例如 `status === 'closed'`，严禁 `status === '已结清'`）。
   - 中文只允许出现在界面展示、提示信息和映射表转换后的标签中；展示文案变化不得影响业务逻辑。
   - 新增或修改状态、渠道、角色等字段时，必须同时定义稳定的英文枚举值与中文标签映射，并在接口、云函数和前端保持一致。

2. **云函数部署与数据库变更主动提醒（强制）**：
   - 每次修改代码后，若涉及到更新云函数（如修改或新增 `cloudfunctions/` 下的逻辑）或需要新增/修改数据库集合与索引，代码编写完成后**必须主动明确告知用户**需要上传/更新哪个云函数或调整哪些数据库结构，保证用户可以顺畅调试。

3. **数据库设计文档同步（强制）**：
   - 后续开发中若新增、删除或变更数据库集合（表）及核心字段，必须同步在数据库设计方案文件（`/database/DATABASE_DESIGN.md`）中补充/更新其集合名称、作用说明与字段规范，保证文档与数据库结构始终一致。

4. **严禁硬编码（强制）**：
   - 严禁在页面组件、业务代码或各云函数逻辑中散落硬编码（如微信订阅消息模板 ID、密钥、Token、环境 ID、特殊业务配置 ID 或重复配置项）。
   - 所有全局配置、模板 ID、系统密钥、数据库集合名或全局常量，必须统一抽离至专用的配置文件（如 `utils/wechat-subscription.js`、`utils/dictionary.js`、`.env` 或统一的常量模块）集中定义与维护，修改时只能在一个地方统一调整，严禁在多处代码中重复硬编码。

5. **文件安全防护**：
   - 未经用户明确同意，AI 严禁擅自删除用户创建的文件或源码文件。

---

## 三、 通用代码规范

1. **命名规范**：
   - 变量与函数：小驼峰，如 `userList`、`getUserInfo`
   - Vue 组件/页面：大驼峰，如 `UserManage.vue`
   - 文件与目录：短横线连接，如 `user-manage`
   - 常量：全大写加下划线，如 `SESSION_TIMEOUT_MINUTES`
   - 接口与数据库字段：小写加下划线，如 `user_id`、`create_time`
2. **代码格式与编码**：
   - 缩进统一使用 2 空格；字符串使用单引号；语句末尾加分号。
   - 所有文件保存统一使用 **UTF-8 无 BOM 格式**，严禁出现中文乱码。
3. **代码注释**：
   - 函数和关键逻辑必须编写清晰的中文注释，说明功能、入参、出参及异常处理。
4. **异常与安全**：
   - 所有异步调用、接口请求与数据库操作必须使用 `try/catch` 包裹，并提供友好的错误提示。
   - 严禁在代码中硬编码密钥、Token 或敏感环境 ID，统一通过 `.env` 或环境变量配置。
