# 全局编程规则

## 业务判断与展示文案分离

- 禁止使用中文展示文案作为业务判断、状态比较、权限判断、分支条件或数据库枚举值。
- 所有业务判断必须使用稳定的英文枚举值、布尔值、ID 或常量，例如 `status === 'closed'`，而不是 `status === '已结清'`。
- 中文只允许出现在界面展示、提示信息和通过映射表转换后的标签中；展示文案变化不得影响业务逻辑。
- 新增或修改状态、渠道、角色等字段时，必须同时定义稳定的值与中文标签映射，并在接口、云函数和前端保持一致。

## 云函数部署与数据库变更主动提醒

- 每次修改代码后，若涉及到更新云函数（如修改或新增 `cloudfunctions/` 下的逻辑）或需要新增/修改数据库集合与索引，代码编写完成后**必须主动明确告知用户**需要上传/更新哪个云函数或调整哪些数据库结构，保证用户可以顺畅调试。
